---
layout: base
title: Pete Lambert
---

<div class="container hero hero--homepage" id="hero">
  <section class="hero__content">
    {% set release = releases | reverse | first %}
      {% if release.purchaseUrl %}
        <a  
          href="{{ release.purchaseUrl }}" 
          target="_blank" 
          rel="noopener noreferrer"
          >
          <img 
            class="hero__art" 
            src="{%  imageUrlFor release.artwork, 900  %}" 
            alt="{{ release.title }} Cover Art"
          >
        </a>
      {% else %}
        <img 
          class="hero__art" 
          src="{%  imageUrlFor release.artwork, 900  %}" 
          alt="{{ release.title }} Cover Art"
        >
      {% endif %}
    
    <div class="hero__text">
      
			
		
				
        {% if release.title %}
          <h1 class="hero__title">{{ release.title }}</h1>
        {% endif %}
        {% if release.subtitle %}
          <h2 class="hero__subtitle">{{ release.subtitle }}</h2>
        {% endif %}
        {% if release.spotifyEmbedUrl %}
          <div class="release__spotify">
            <iframe style="border-radius:12px" src="{{ release.spotifyEmbedUrl }}" width="100%" height="152" frameBorder="0" allowfullscreen="" allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy"></iframe>
            </iframe>
          </div>
        {% endif %}
        
					<a 
						class="button hero__presave" 
						href="/music/{{ release.slug }}">
						Get it now
					</a>
				
        
    </div>
  </section>
</div>

<div class="container" id="bio">
  <section class="container__content">
    {% include "bio.njk" %}
    {% include "social-icons.njk" %}
  </section>
</div>


<div class="container container--2" id="press">
  <section class="container__content">
    <h1 class="title">Press for Pete Lambert</h1>
    {% include "quoteslist.njk" %}
  </section>
</div>

<div class="container container" id="press">
  <section class="container__content">
    <h1 class="title">Upcoming Shows</h1>
    {% include "showslist.njk" %}
  </section>
</div>
